spring:
  datasource:
    url: jdbc:mysql:///blog?useUnicode=true&characterEncoding=utf8
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: true

logging:
  level:
    root: info
    com.sher.simpleblog: debug
  file:
    name: log/blog-dev.log

server:
  port: 8080
